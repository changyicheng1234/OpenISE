#include <stdio.h>

const int maxn = 1000000;

int m, n;
int a[maxn + 1];
int b[maxn + 1];
int c[maxn * 2 + 1];


int main() {
	scanf("%d%d", &m, &n);
	for (int i = 0; i < m; i++) scanf("%d", &a[i]);
	for (int i = 0; i < n; i++) scanf("%d", &b[i]);
	
    //你需要完成这一段代码

	for (int i = 0; i < s; i++)
		printf("%d ", c[i]);

	return 0;
}
